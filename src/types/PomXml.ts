
export default interface PomXml {
    parent: {
        groupId: string;
        artifactId: string;
        version: string;
    },
    groupId: string;
    artifactId: string;
    version: string;
}
