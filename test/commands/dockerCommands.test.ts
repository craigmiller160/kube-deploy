import getCwd from '../../src/utils/getCwd';

describe('dockerCommands', () => {
    it('dockerBuild', () => {
        throw new Error();
    });

    it('dockerPush', () => {
        throw new Error();
    });
});
