import path from 'path';

describe('kubeCommands', () => {
    it('applyConfigMap', () => {
        throw new Error();
    });

    it('applyDeployment', () => {
        throw new Error();
    });
});
