import path from 'path';

describe('validateArtifactVersion', () => {
    it('version is valid', () => {
        throw new Error();
    });

    it('version is not valid', () => {
        throw new Error();
    });
});
