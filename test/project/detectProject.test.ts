import path from 'path';

describe('detectProject', () => {
    it('is JS project', () => {
        throw new Error();
    });

    it('is maven project', () => {
        throw new Error();
    });

    it('is unknown project', () => {
        throw new Error();
    });
});
